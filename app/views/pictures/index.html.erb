<section class="main-header">
  <p class="flash"><%= notice %><%= alert %></p>
</section>

<div class="container-fluid">
  <div class="row">
    <% @pictures.each do |picture| %>
      <div class="one-picture col-lg-6 col-lg-offset-3 col-md-8 col-md-offset-2 col-sm-10 col-sm-offset-1 col-xs-12">
          <p><%= profile_img(picture.user)%> <%= picture.user.name%></p>
          <p><%= image_tag picture.image.url %></p>
          <p><%= picture.content %></p>
          <%= link_to "編集", edit_picture_path(picture.id), class: 'btn btn-default btn-sm btn-success' %>
          <%= link_to "削除", picture_path(picture.id), method: :delete, data: { confirm: '本当に削除していいですか？'}, class: 'btn btn-default btn-sm btn btn-danger' %>
      </div>
    <%end%>
  </div>
</div>
